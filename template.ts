const input = `A Y
B X
C Z`
console.log('input')
// const fs = require("fs");
const rounds = input.split("\n");
const isWinner = (opp: String, you) => {
    console.log(opp.charCodeAt(0))
}
rounds.map(isWinner)
